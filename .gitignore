# secrets — never commit
.env

# virtualenv / caches
.venv/
__pycache__/
*.pyc

# runtime artifacts (repo-root only — NOT the scanner/data source package)
# runtime artifacts (repo root only — do NOT ignore scanner/data/ package)
/logs/
/data/
